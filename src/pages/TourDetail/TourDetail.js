import classNames from 'classnames/bind';
import styles from './TourDetail.module.scss';

const cx = classNames.bind(styles);

function TourDetail() {
    return <h1> TourDetail Page</h1>;
}

export default TourDetail;
